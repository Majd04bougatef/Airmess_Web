<form method="post" action="{{ path('app_user_delete', {'id_U': user.idU}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.idU) }}">
    <button class="btn">Delete</button>
</form>
