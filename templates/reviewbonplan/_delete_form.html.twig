<form method="post" action="{{ path('app_reviewbonplan_delete', {'idR': reviewbonplan.idR}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reviewbonplan.idR) }}">
    <button class="btn">Delete</button>
</form>
