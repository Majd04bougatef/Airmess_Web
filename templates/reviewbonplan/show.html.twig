{% extends 'base.html.twig' %}

{% block title %}ReviewBonPlan{% endblock %}

{% block body %}
    <h1>ReviewBonPlan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdR</th>
                <td>{{ reviewbonplan.idR }}</td>
            </tr>
            <tr>
                <th>Id_U</th>
                <td>{{ reviewbonplan.idU }}</td>
            </tr>
            <tr>
                <th>Commente</th>
                <td>{{ reviewbonplan.commente }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ reviewbonplan.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reviewbonplan_index') }}">back to list</a>

    <a href="{{ path('app_reviewbonplan_edit', {'idR': reviewbonplan.idR}) }}">edit</a>

    {{ include('reviewbonplan/_delete_form.html.twig') }}
{% endblock %}
