<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Ticket de réservation - {{ reservation.reference }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .ticket {
            border: 2px solid #5e72e4;
            border-radius: 10px;
            padding: 20px;
            max-width: 500px;
            margin: 0 auto;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .logo {
            width: 100px;
            margin-bottom: 10px;
        }
        .title {
            color: #5e72e4;
            font-size: 24px;
            margin: 10px 0;
        }
        .reference {
            background-color: #f8f9fa;
            padding: 10px;
            border-radius: 5px;
            text-align: center;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .details {
            margin-bottom: 20px;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding-bottom: 10px;
            border-bottom: 1px dashed #ddd;
        }
        .label {
            font-weight: bold;
            color: #344767;
        }
        .value {
            color: #67748e;
        }
        .qr-code {
            text-align: center;
            margin: 20px 0;
        }
        .qr-code img {
            width: 150px;
            height: 150px;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #67748e;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="ticket">
        <div class="header">
            <img src="{{ asset('images/signalisation (2).png') }}" alt="Logo" class="logo">
            <h1 class="title">Ticket de Réservation</h1>
        </div>
        
        <div class="reference">
            Référence: {{ reservation.reference }}
        </div>
        
        <div class="details">
            <div class="detail-row">
                <span class="label">Station:</span>
                <span class="value">{{ reservation.station.nom }}</span>
            </div>
            <div class="detail-row">
                <span class="label">Date de début:</span>
                <span class="value">{{ reservation.dateRes|date('d/m/Y à H:i') }}</span>
            </div>
            <div class="detail-row">
                <span class="label">Date de fin:</span>
                <span class="value">{{ reservation.dateFin|date('d/m/Y à H:i') }}</span>
            </div>
            <div class="detail-row">
                <span class="label">Nombre de vélos:</span>
                <span class="value">{{ reservation.nombreVelo }}</span>
            </div>
            <div class="detail-row">
                <span class="label">Prix total:</span>
                <span class="value">{{ reservation.prix }}€</span>
            </div>
        </div>
        
        <div class="qr-code">
            <img src="{{ asset('images/qr-code-example.png') }}" alt="QR Code" onerror="this.src='https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=ReservationBike{{ reservation.reference }}'; this.onerror='';">
        </div>
        
        <div class="footer">
            <p>Présentez ce ticket à la station pour récupérer vos vélos</p>
            <p>© {{ "now"|date("Y") }} Airmess. Tous droits réservés.</p>
        </div>
    </div>
</body>
</html> 