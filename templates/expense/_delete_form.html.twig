<form method="post" action="{{ path('app_expense_delete', {'idE': expense.idE}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expense.idE) }}">
    <button class="btn">Delete</button>
</form>
