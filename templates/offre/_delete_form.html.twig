<form method="post" action="{{ path('app_offre_delete', {'idO': offre.idO}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.idO) }}">
    <button class="btn">Delete</button>
</form>
